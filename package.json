{
  "name": "kiracode-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "KIRACODE_PORT=3141 VITE_KIRACODE_API_URL=http://127.0.0.1:3141/trpc turbo run dev --filter=kiracode --filter=@kiracode/frontend",
    "dev:backend": "KIRACODE_PORT=3141 turbo run dev --filter=kiracode",
    "dev:frontend": "VITE_KIRACODE_API_URL=http://127.0.0.1:3141/trpc turbo run dev --filter=@kiracode/frontend",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint apps packages",
    "lint:fix": "oxlint --fix apps packages",
    "format": "oxfmt --check apps packages",
    "format:fix": "oxfmt apps packages"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "turbo": "^2.8.20",
    "typescript": "^5.8.2",
    "vitest": "^4.1.0"
  }
}
